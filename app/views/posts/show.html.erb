<div class="col-md-12">
  <h2 class="text-center">
    Group - <%= @group.title %>
  </h2>
  <p><%= render_post_content(@post) %></p>
  <%= link_to("Back", group_path(@group), class: 'btn btn-default') %>
</div>
